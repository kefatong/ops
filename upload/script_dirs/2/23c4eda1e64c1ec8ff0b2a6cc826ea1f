- hosts: all
  gather_facts: yes
  remote_user: kefatong
  tasks:
    - name: test connection
      command: ls /tmp/
      register: tmp_dirs

    - name: check contents for emptiness
      debug: msg="Directory is empty"
      when: tmp_dirs.stdout == ""
